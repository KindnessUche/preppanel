# Copy this to .env.local and adjust if your backend runs somewhere other than
# the default. NEXT_PUBLIC_ vars are exposed to the browser, which is required
# here since all API calls happen client-side.

NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
